# AGENTS.md

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues at `github.com/gvansh24/chess-app`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical 5-role vocabulary, 1:1 mapping (no renames). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
